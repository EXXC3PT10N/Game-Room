<Page
    x:Class="GameRoom.MenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameRoom"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Button x:Name="btnTetris" HorizontalAlignment="Center" VerticalAlignment="Top" Height="250" Margin="-300,0,0,0" Width="250" Click="BtnTetris_Click" Padding="0">
            <Image Source="Assets/tetris.png" />
        </Button>
        <Button x:Name="btnTicTacToe"  HorizontalAlignment="Center" VerticalAlignment="Top" Height="250" Margin="300,0,0,0" Width="250" Click="BtnTicTacToe_Click" Padding="0">
            <Image Source="Assets/tictactoe.png" />
        </Button>
        <Button x:Name="btnAboutApp" Content="O aplikacji" HorizontalAlignment="Center" VerticalAlignment="Top" Height="100" Margin="0,271,0,0" Width="250" Click="BtnAboutApp_Click"/>
        <Button x:Name="btnAboutMe" Content="O autorze" HorizontalAlignment="Center" VerticalAlignment="Top" Height="100" Margin="0,376,0,0" Width="250" Click="BtnAboutMe_Click"/>
        <Button x:Name="btnExit" Content="Exit" HorizontalAlignment="Center" VerticalAlignment="Top" Height="100" Margin="0,481,0,0" Width="250" Click="BtnExit_Click"/>
    </Grid>
</Page>
